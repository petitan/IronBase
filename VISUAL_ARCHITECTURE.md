# MongoLite - Vizuális Architektúra

## Rendszer Áttekintő Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                      PYTHON ALKALMAZÁS                          │
│                                                                 │
│  from mongolite import MongoLite                                │
│  db = MongoLite("myapp.mlite")                                  │
│  users = db.collection("users")                                 │
│  users.insert_one({"name": "János", "age": 30})                 │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ PyO3 Binding
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                      PYTHON API LAYER                           │
│                        (PyO3 Wrapper)                           │
│                                                                 │
│  ┌──────────────┐         ┌──────────────┐                     │
│  │  MongoLite   │         │ Collection   │                     │
│  │   @pyclass   │────────▶│  @pyclass    │                     │
│  └──────────────┘         └──────────────┘                     │
│       │                          │                              │
│       │ open()                   │ insert_one()                 │
│       │ collection()             │ insert_many()                │
│       │ close()                  │ find()                       │
│                                  │ update_one()                 │
│                                  │ delete_one()                 │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ Rust FFI
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                      RUST CORE LIBRARY                          │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │            COLLECTION MANAGER                           │   │
│  │  • insert_one(), insert_many()                          │   │
│  │  • find(), find_one()                                   │   │
│  │  • update_one(), update_many()                          │   │
│  │  • delete_one(), delete_many()                          │   │
│  └────────────────┬───────────────────────────────────────┘   │
│                   │                                             │
│  ┌────────────────▼───────────────────────────────────────┐   │
│  │            QUERY ENGINE                                │   │
│  │  • Query parsing (MongoDB syntax)                      │   │
│  │  • Operator matching ($gt, $lt, $in, $eq)             │   │
│  │  • Logical operators ($and, $or, $not)                │   │
│  │  • Document filtering                                  │   │
│  └────────────────┬───────────────────────────────────────┘   │
│                   │                                             │
│  ┌────────────────▼───────────────────────────────────────┐   │
│  │            INDEX MANAGER                               │   │
│  │  • B-tree indexes                                      │   │
│  │  • Unique constraints                                  │   │
│  │  • Index-based lookup                                  │   │
│  │  • create_index(), drop_index()                        │   │
│  └────────────────┬───────────────────────────────────────┘   │
│                   │                                             │
│  ┌────────────────▼───────────────────────────────────────┐   │
│  │            DOCUMENT HANDLER                            │   │
│  │  • Document structure (JSON)                           │   │
│  │  • ID generation (auto-increment, ObjectId)            │   │
│  │  • Serialization/Deserialization                       │   │
│  └────────────────┬───────────────────────────────────────┘   │
└───────────────────┼─────────────────────────────────────────────┘
                    │
                    │
┌───────────────────▼─────────────────────────────────────────────┐
│                      STORAGE ENGINE                             │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │            MEMORY-MAPPED I/O                            │   │
│  │  • Fast file access (mmap)                              │   │
│  │  • OS-level caching                                     │   │
│  │  • Efficient reads/writes                               │   │
│  └────────────────┬───────────────────────────────────────┘   │
│                   │                                             │
│  ┌────────────────▼───────────────────────────────────────┐   │
│  │            FILE MANAGER                                │   │
│  │  • Append-only log                                     │   │
│  │  • Header management (128 bytes)                       │   │
│  │  • Collection metadata                                 │   │
│  │  • Free space tracking                                 │   │
│  └────────────────┬───────────────────────────────────────┘   │
└───────────────────┼─────────────────────────────────────────────┘
                    │
                    │ File I/O
                    │
┌───────────────────▼─────────────────────────────────────────────┐
│                    .mlite FILE (DISK)                           │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ HEADER (128 bytes)                                       │  │
│  │  Magic: "MONGOLTE"                                       │  │
│  │  Version: 1                                              │  │
│  │  Page size: 4096                                         │  │
│  │  Collection count: N                                     │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │ COLLECTION METADATA (variable size)                      │  │
│  │  Collection 1: {name, doc_count, offsets, last_id}      │  │
│  │  Collection 2: {name, doc_count, offsets, last_id}      │  │
│  │  ...                                                     │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │ DOCUMENT DATA (append-only)                              │  │
│  │  [4B length][JSON document]                              │  │
│  │  [4B length][JSON document]                              │  │
│  │  [4B length][JSON document]                              │  │
│  │  ...                                                     │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │ INDEX DATA (B-tree structures)                           │  │
│  │  Index 1: B-tree nodes                                   │  │
│  │  Index 2: B-tree nodes                                   │  │
│  │  ...                                                     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Size: Unlimited (OS maximum: ~16 Exabytes)                    │
└─────────────────────────────────────────────────────────────────┘
```

## Adatfolyam - INSERT művelet

```
Python App
    │
    │ users.insert_one({"name": "János", "age": 30})
    ▼
PyO3 Layer
    │
    │ Python dict → Rust HashMap
    ▼
Collection Manager
    │
    │ 1. Generate ID (auto-increment)
    │ 2. Create Document struct
    ▼
Document Handler
    │
    │ Serialize to JSON
    ▼
Storage Engine
    │
    │ 1. Seek to end of file
    │ 2. Write length prefix (4 bytes)
    │ 3. Write JSON data
    │ 4. Update metadata
    │ 5. Flush to disk
    ▼
.mlite File
    │
    │ [SUCCESS] Document saved
    ▼
Return to Python
    │
    │ {"acknowledged": True, "inserted_id": 1}
```

## Adatfolyam - FIND művelet (tervezett)

```
Python App
    │
    │ users.find({"age": {"$gt": 25}})
    ▼
PyO3 Layer
    │
    │ Python dict → Rust Query
    ▼
Query Engine
    │
    │ 1. Parse query operators
    │ 2. Check for applicable indexes
    ▼
Index Manager (if index exists)
    │
    │ B-tree lookup → Document IDs
    ▼
OR
    │
Collection Manager (full scan)
    │
    │ Iterate all documents
    ▼
Storage Engine
    │
    │ 1. Read documents
    │ 2. Apply query filter
    │ 3. Collect matches
    ▼
Return to Python
    │
    │ [List of matching documents]
```

## Komponensek Kapcsolata

```
┌─────────────────┐
│   Python App    │  ← Felhasználó kód
└────────┬────────┘
         │
    ┌────▼─────┐
    │   PyO3   │  ← Rust-Python híd
    └────┬─────┘
         │
    ┌────▼─────────────┐
    │  Rust Core       │  ← Üzleti logika
    │  • Collection    │
    │  • Query         │
    │  • Index         │
    │  • Document      │
    └────┬─────────────┘
         │
    ┌────▼─────────────┐
    │ Storage Engine   │  ← Adatkezelés
    │  • mmap I/O      │
    │  • File manager  │
    └────┬─────────────┘
         │
    ┌────▼─────────────┐
    │  .mlite File     │  ← Perzisztencia
    └──────────────────┘
```

## Teljesítmény Karakterisztika

```
Művelet         │ Idő (MVP)    │ Idő (Optimalizált)
────────────────┼──────────────┼────────────────────
insert_one      │ < 1ms        │ < 100µs
insert_many(100)│ < 100ms      │ < 10ms
find (scan)     │ 1000 doc/ms  │ 5000 doc/ms
find (index)    │ N/A          │ < 1ms
update_one      │ < 2ms        │ < 500µs
delete_one      │ < 2ms        │ < 500µs
count           │ instant      │ instant
```

## Fájlméret Növekedés

```
Dokumentumok    │ Átlagos fájlméret │ Megjegyzés
────────────────┼───────────────────┼─────────────────
0               │ 1 KB              │ Header + metadata
1,000           │ ~500 KB           │ ~500B/doc
10,000          │ ~5 MB             │
100,000         │ ~50 MB            │
1,000,000       │ ~500 MB           │ Indexek: +20%
10,000,000      │ ~5 GB             │ Ajánlott max
```

## Összehasonlítás

```
                │ MongoLite    │ SQLite       │ MongoDB
────────────────┼──────────────┼──────────────┼─────────────
Méret           │ ~2 MB        │ ~1 MB        │ ~500 MB
Fájlok száma    │ 1            │ 1            │ Sok
Setup           │ Zero-config  │ Zero-config  │ Komplex
API             │ MongoDB-like │ SQL          │ MongoDB
Skálázhatóság   │ Egy gép      │ Egy gép      │ Cluster
Use case        │ Embedded     │ Embedded     │ Production
```

---

## Kulcs Tanulságok

1. **Egyszerűség**: Egyetlen fájl, zero-config
2. **Teljesítmény**: Rust backend, mmap I/O
3. **Használhatóság**: MongoDB API Python-ból
4. **Rugalmasság**: JSON dokumentumok, dinamikus séma
5. **Méretezhetőség**: OS maximum fájlméretig

**MongoLite = Best of Both Worlds** 🚀
